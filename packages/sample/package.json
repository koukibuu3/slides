{
  "name": "sample",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "slidev --open",
    "build": "slidev build --out ../../dist/sample --base /slides/sample",
    "export": "slidev export --output ../output/example.pdf",
    "export:pptx": "slidev export --output ../output/example --format pptx"
  }
}
